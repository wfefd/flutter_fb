// lib/core/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // 전체 앱 배경 (밝은 회색)
  static const background = Color(0xFFF5F5F5);

  // 카드나 컨테이너, AppBar 같은 영역의 바탕 (약간 더 진한 회색)
  static const surface = Color(0xFFEEEEEE);

  // 경계선, Divider 등 얇은 라인용
  static const border = Color(0xFFDDDDDD);

  // 주 텍스트 색상
  static const primaryText = Color(0xFF393E46);

  // 보조 텍스트 색상
  static const secondaryText = Color(0xFF929AAB);
}
