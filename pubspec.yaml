name: flutter_fb
description: "A new Flutter project."

# 앱 버전
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase 관련 패키지
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2

  # iOS 스타일 아이콘
  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.1
  intl: ^0.20.2
  flutter_native_splash: ^2.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 권장 린트 규칙
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # 📂 폰트 및 이미지 폴더
    - assets/fonts/
    - assets/images/
    - assets/images/jobs/
    - assets/images/items/

    # 🔹 주요 이미지
    - assets/images/logo_done.png
    - assets/images/logo_done_big.png
    - assets/images/fame.png
    - assets/images/sample_weapon.png

    # 🔹 스탯 아이콘 폴더 전체
    - assets/images/stat/

  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/Pretendard-Regular.ttf
        - asset: assets/fonts/Pretendard-Medium.ttf
        - asset: assets/fonts/Pretendard-SemiBold.ttf
        - asset: assets/fonts/Pretendard-Bold.ttf

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/logo_done_big.png
  android: true
  ios: true
  web: false
