name: flutter_fb
description: "A new Flutter project."

# 앱 버전
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase 관련 패키지
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2

  # iOS 스타일 아이콘
  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.1
  intl: ^0.20.2
  flutter_native_splash: ^2.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  # 권장 린트 규칙
  flutter_lints: ^6.0.0
flutter:
  # 📦 기본 머티리얼 아이콘 사용
  uses-material-design: true

  # 🖼️ 프로젝트에 포함된 이미지 리소스 (assets)
  assets:
    - assets/fonts/
    - assets/images/
    - assets/images/jobs/
    - assets/images/items/
    - assets/images/logo_done.png
    - assets/images/logo_done_big.png
    - assets/images/fame.png
    - assets/images/sample_weapon.png
  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/Pretendard-Regular.ttf
        - asset: assets/fonts/Pretendard-Medium.ttf
        - asset: assets/fonts/Pretendard-SemiBold.ttf
        - asset: assets/fonts/Pretendard-Bold.ttf
    # 예: assets/images/character1.png
    #     assets/images/character2.png
    #     assets/images/no_image.png

  # ✏️ (선택) 커스텀 폰트 추가 예시
  # fonts:
  #   - family: NanumSquare
  #     fonts:
  #       - asset: assets/fonts/NanumSquare-Regular.ttf
  #       - asset: assets/fonts/NanumSquare-Bold.ttf
  #         weight: 700

flutter_native_splash:
  color: "#FFFFFF" # 배경색
  image: assets/images/logo_done_big.png  # ✅ 띄울 로고 경로
  android: true  # 안드로이드용 스플래시 생성
  ios: true      # iOS용 스플래시 생성
  web: false     # 웹 버전엔 적용 안 함

 
